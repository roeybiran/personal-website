---
import GithubLogo from "../assets/github-logo.svg";

export interface Props {
  url: string;
  small?: boolean;
}

const { url, small } = Astro.props;
---

<a class="button button--image" class:list={{ "button-s": small }} href={url} target="_blank">
  View on GitHub
  <GithubLogo />
</a>
